describe("when song has been paused", function() {
    beforeEach(function() {

    });
    it("should indicate that the song is currently paused", function() {
        expect(jsObject).toBeTruthy();
    });
});